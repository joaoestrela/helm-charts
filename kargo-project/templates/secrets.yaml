{{- range $index, $secret := .Values.secrets }}
{{- with $secret }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kargo-project.fullname" $ }}-{{ $secret.name }}
  namespace: {{ include "kargo-project.fullname" $ }}
  labels:
    kargo.akuity.io/cred-type: {{ $secret.type }}
{{- with $secret.data }}
stringData:
  {{- toYaml . | nindent 2 }}
{{- end }}
{{- end }}
{{- end }}


